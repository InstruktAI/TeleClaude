# Gemini CLI Agents

## Overview

Gemini CLI does not document a user-managed agent file format (no `.gemini/agents` folder).
Agent behavior can be customized through configuration overrides.

## Configuration

- `agents.overrides` in settings can adjust built-in agent behavior.

## Sources

- https://geminicli.com/docs/get-started/configuration/
