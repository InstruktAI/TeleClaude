Add telec memories CLI command group to replace raw curl examples in baseline. Namespace: telec memories — Manage long-term memory observations. Three subcommands: save (--text, --title, --type, --project), search <query> (--type comma-separated, --project, --limit), delete <id> (positional). Handlers call existing /api/memory/* endpoints. Also replace the agent-restart curl example in baseline with telec sessions restart self.