Add @exec expansions to telec-cli.md baseline spec for content, todo, roadmap, bugs, and events namespaces. Currently only sessions, docs, computers, projects, agents, channels have expanded help in the baseline. The missing namespaces appear in the top-level telec -h summary but their subcommand help is never captured. Add <!-- @exec: telec content -h -->, <!-- @exec: telec todo -h -->, <!-- @exec: telec roadmap -h -->, <!-- @exec: telec bugs -h -->, <!-- @exec: telec events -h --> sections to docs/global/general/spec/tools/telec-cli.md, then telec sync to regenerate. Auth and config are intentionally excluded — not needed in baseline.