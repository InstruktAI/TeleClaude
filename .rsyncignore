# Rsync exclude patterns for TeleClaude
# Prevents overwriting local configuration when syncing between computers
# NOTE: .gitignore is also used as exclude file, only add items NOT in .gitignore

# Local configuration variants
.env.*

# Database variants (*.db is in .gitignore)
*.db-shm
*.db-wal

# Git directory (critical - never sync)
.git/

# IDE files (entire directory, .gitignore only has specific files)
.vscode/

# OS files (Windows)
Thumbs.db

# Temporary files
*.tmp

# Upload directory (could contain sensitive user files)
uploads/

# Session data
session-env/
shell-snapshots/

# MCP socket
*.sock
