"""Shared fixtures for Redis transport characterization tests.

Each test file defines its own fixtures tailored to its module's mock
requirements. This conftest is kept as a placeholder for future shared
setup (e.g. marks, autouse fixtures).
"""
