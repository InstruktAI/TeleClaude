# Input: ucap-tui-adapter-alignment

Parent:

- `unified-client-adapter-pipeline`

Objective:

- Move TUI realtime output progression onto the canonical adapter contract.
- Remove TUI-side bypass semantics for core output updates.

Context:

- Depends on `ucap-truthful-session-status`.
