# Input: ucap-web-adapter-alignment

Parent:

- `unified-client-adapter-pipeline`

Objective:

- Move Web realtime output progression onto the canonical adapter contract.
- Remove direct Web bypass paths for core output delivery.

Context:

- Depends on `ucap-truthful-session-status`.
