# Auth
AUTH_SECRET=  # Generate with: openssl rand -base64 32
AUTH_URL=http://localhost:3000

# Database
DATABASE_URL=./teleclaude-web.db

# SMTP (Brevo) — all required, see parent .env for values
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Daemon
DAEMON_SOCKET_PATH=/tmp/teleclaude-api.sock

# People config
TELECLAUDE_CONFIG_PATH=~/.teleclaude/teleclaude.yml
